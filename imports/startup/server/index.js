// Define all the collections, publications and
// methods that the application provides
// as an API to the client
import './register-api.js';

// Define the accounts configuration for server
// side operations, such as 'onCreateUser',
// etc...
import './accounts-config.js';

// Define the google-accounts clientId and secret
// to prevent setting it up every time
import './accounts.js';

// Push configuration for server-side
import './push-configuration.js';

// Insert the first team for Carlos y Dario
import './insert-team.js';
